{% extends 'base.html.twig' %}
 {% block body %}

 	<H2>Article de la catégorie : {{ articles[0].category.name }} </H2>

 	{% for article in articles %}
 	<h4>Titre : {{ article.title }}</h4>
 		<p>{{ article.content }}</p>
 	{% endfor %}

 {% endblock %}